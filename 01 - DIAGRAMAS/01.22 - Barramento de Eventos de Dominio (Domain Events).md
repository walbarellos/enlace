# 01.22 — BARRAMENTO DE EVENTOS DE DOMÍNIO (DOMAIN EVENTS)

> **Documento:** 01.22  
> **Fase:** Modelagem e Arquitetura (Modelo em Cascata)  
> **Classificação:** Arquitetura de Software / Event-Driven Architecture  
> **Versão:** 2.0.0  
> **Status:** Aprovado  

---

## 1. Coreografia de Eventos Internos do Monólito

O diagrama a seguir rastreia os principais eventos de domínio disparados pelas ações centrais da plataforma e seus respectivos ouvintes (*Handlers*):

```mermaid
flowchart TD
    subgraph EmissoresEventos["Eventos de Domínio Emitidos"]
        E1["<b>IdentityVerifiedEvent</b><br/>(KYC Civil Aprovado +18)"]
        E2["<b>PaymentConfirmedEvent</b><br/>(Liquidação de Pix Confirmada)"]
        E3["<b>RequestCreatedEvent</b><br/>(Cartão de Solicitação Emitido)"]
        E4["<b>SubscriptionActivatedEvent</b><br/>(Plano de Destaque Pago)"]
        E5["<b>SafetyReportCreatedEvent</b><br/>(Denúncia / Alerta de Risco)"]
        E6["<b>ReviewSubmittedEvent</b><br/>(Avaliação Bilateral Enviada)"]
    end

    subgraph BarramentoInterno["In-Process Event Bus (Event-Driven)"]
        Bus["<b>Mediador de Eventos em Memória</b><br/>(Assíncrono / Não-bloqueante)"]
    end

    subgraph HandlersReceptores["Ouvintes de Domínio (Handlers)"]
        H1["ProfilesHandler: Ativa Selo Verificado na Vitrine"]
        H2["CommerceHandler: Executa Split Contábil (85% / 15%)"]
        H3["ContentHandler: Concede Entitlement de Acesso à Mídia"]
        H4["MediaWorker: Agenda Injeção de Esteganografia"]
        H5["DiscoveryHandler: Aplica Multiplicador de Ranking no Redis"]
        H6["TrustHandler: Alimenta Motor Bayesiano e Avalia Quarentena"]
        H7["ReviewsHandler: Checa Janela Cega (Double-Blind)"]
    end

    %% Ligações
    E1 --> Bus
    E2 --> Bus
    E3 --> Bus
    E4 --> Bus
    E5 --> Bus
    E6 --> Bus

    Bus --> H1
    Bus --> H2
    Bus --> H3
    Bus --> H4
    Bus --> H5
    Bus --> H6
    Bus --> H7

    E1 -.-> H1
    E2 -.-> H2
    E2 -.-> H3
    E2 -.-> H4
    E4 -.-> H5
    E5 -.-> H6
    E6 -.-> H7
```

---

## 2. Padrão Outbox para Consistência Transacional

Para garantir que nenhum evento seja perdido em caso de reinicialização ou falha de infraestrutura durante uma transação financeira crítica, o sistema implementa o **Transactional Outbox Pattern**:

1. A gravação da ordem paga e a emissão do `PaymentConfirmedEvent` ocorrem dentro da **mesma transação SQL** no PostgreSQL;
2. O evento é persistido na tabela `outbox_events`;
3. Um leitor assíncrono consome a tabela e entrega o evento aos handlers, garantindo entrega do tipo *at-least-once*.
