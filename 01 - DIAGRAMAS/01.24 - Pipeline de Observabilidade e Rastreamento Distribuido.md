# 01.24 — PIPELINE DE OBSERVABILIDADE E RASTREAMENTO DISTRIBUÍDO

> **Documento:** 01.24  
> **Fase:** Modelagem e Arquitetura (Modelo em Cascata)  
> **Classificação:** Engenharia de Confiabilidade / SRE & APM  
> **Versão:** 2.0.0  
> **Status:** Aprovado  

---

## 1. Topologia da Telemetria Unificada (OpenTelemetry)

O diagrama a seguir descreve a coleta, higienização de dados pessoais e agregação de métricas, traces e logs em tempo real:

```mermaid
flowchart TD
    subgraph FontesTelemetria["Emissores de Telemetria"]
        WebClient["Frontend Next.js (RUM OpenTelemetry SDK)"]
        APICore["Backend Core (OTel Auto-Instrumentation)"]
        MediaWorker["Media Worker (FFmpeg Metrics)"]
        PostgreSQL["PostgreSQL Engine (pg_stat_statements)"]
    end

    subgraph PerimetroHigienizacao["Camada de Sanitização de Borda"]
        Scrubber["Log & PII Sanitizer Middleware<br/><i>(Mascara CPFs, E-mails e Chaves Pix)</i>"]
    end

    subgraph ColetorCentral["Agregação Centralizada"]
        OTelCollector["<b>OpenTelemetry Collector Cluster</b><br/>(Processa, agrupa e exporta métricas, traces e logs)"]
    end

    subgraph BackendsObservabilidade["Armazenamento e Visualização"]
        Prometheus[("Prometheus Cluster<br/>(Métricas & Histogramas SLO)")]
        JaegerTempo[("Grafana Tempo / Jaeger<br/>(Spans de Rastreamento Distribuído)")]
        GrafanaLoki[("Grafana Loki<br/>(Logs Estruturados JSON Higienizados)")]
        GrafanaPainel["<b>Dashboards Grafana Unificados</b><br/>• Dashboard de SLOs e Latência P95<br/>• Funil de Conversão Pix e Splits<br/>• Mapa de Calor de Tráfego de Acessibilidade"]
        Alertmanager["Alertmanager (Telegram / PagerDuty)"]
    end

    WebClient -->|"Traces W3C TraceContext"| APICore
    APICore --> Scrubber
    Scrubber --> OTelCollector
    MediaWorker --> OTelCollector
    PostgreSQL --> OTelCollector

    OTelCollector --> Prometheus
    OTelCollector --> JaegerTempo
    OTelCollector --> GrafanaLoki

    Prometheus --> GrafanaPainel
    JaegerTempo --> GrafanaPainel
    GrafanaLoki --> GrafanaPainel
    Prometheus --> Alertmanager
```

---

## 2. Propagação de Contexto W3C TraceContext

Para rastrear qualquer lentidão ou anomalia operacional ponta a ponta:
1. O navegador injeta o cabeçalho padronizado `traceparent: 00-4bf92f3577b34da6a3ce929d0e0e4736-00f067aa0ba902b7-01`;
2. O API Gateway propaga o identificador para o Monólito Modular;
3. As queries ao PostgreSQL recebem o ID como comentário SQL (`/* trace_id=... */`);
4. Em caso de falha no checkout Pix, os operadores localizam a requisição em segundos sem inspecionar dados íntimos do cliente.
