# 01.05 — ARQUITETURA C4 — NÍVEL 3: COMPONENTES DO CORE DA APLICAÇÃO

> **Documento:** 01.05  
> **Fase:** Modelagem e Arquitetura (Modelo em Cascata)  
> **Classificação:** Arquitetura C4 / Nível 3  
> **Versão:** 1.0.0  
> **Status:** Aprovado  

---

## 1. Diagrama C4 — Nível 3: Componentes do Monólito Modular

O diagrama a seguir detalha a organização interna dos módulos (*Bounded Contexts*) que operam dentro do container **Enlace Backend Core**:

```mermaid
flowchart TB
    %% Gateway de Entrada
    APIRoutes["<b>API Router / Controladores REST</b><br/>[Módulo de Entrada]<br/>Roteia requisições HTTP, valida payloads com Zod e aplica Rate Limiting."]

    subgraph BoundedContexts["Bounded Contexts Internos (Módulos de Domínio)"]
        IdentityComp["<b>Identity & Access Component</b><br/>Gerencia credenciais, sessões JWT e geração de pseudônimos de clientes."]
        KYCComp["<b>KYC & Verification Component</b><br/>Orquestra verificação de maioridade (+18), liveness e cofre civil."]
        ProfileComp["<b>Provider Profile Component</b><br/>Gere perfil artístico, galerias públicas, tarifas e agenda semanal."]
        AccessComp["<b>Accessibility Engine Component</b><br/>Calcula índice de compatibilidade entre necessidades e comodidades."]
        GeoComp["<b>GeoDiscovery Component</b><br/>Executa consultas espaciais e aplica ofuscação mandatória de 500m."]
        RequestComp["<b>Request Card Component</b><br/>Gera cartões de intenção e deep links estruturados para canais externos."]
        PaywallComp["<b>Media Paywall Component</b><br/>Controla acesso a mídias pagas, gera URLs assinadas e valida compras."]
        CommerceComp["<b>Commerce & Split Component</b><br/>Gere planos de destaque e orquestra liquidação financeira com split via Pix."]
        TrustComp["<b>Trust & Reviews Component</b><br/>Processa avaliações bilaterais e alimenta o score confidencial de risco."]
        ModComp["<b>Reactive Moderation Component</b><br/>Aplica veto a hashes proibidos e gerencia quarentena por denúncias."]
    end

    %% Persistência
    PostgresDB[("PostgreSQL 16")]
    RedisCache[("Redis 7 Store")]

    %% Roteamento
    APIRoutes --> IdentityComp
    APIRoutes --> KYCComp
    APIRoutes --> ProfileComp
    APIRoutes --> AccessComp
    APIRoutes --> GeoComp
    APIRoutes --> RequestComp
    APIRoutes --> PaywallComp
    APIRoutes --> CommerceComp
    APIRoutes --> TrustComp
    APIRoutes --> ModComp

    %% Interações de Domínio
    GeoComp --> AccessComp
    PaywallComp --> CommerceComp
    TrustComp --> ModComp
    KYCComp --> IdentityComp

    %% Acesso a Dados
    IdentityComp --> PostgresDB
    ProfileComp --> PostgresDB
    AccessComp --> PostgresDB
    GeoComp --> PostgresDB
    CommerceComp --> PostgresDB
    TrustComp --> PostgresDB
    ModComp --> PostgresDB

    IdentityComp --> RedisCache
    GeoComp --> RedisCache
```

---

## 2. Comunicação Inter-Componentes

1. **Invocação Direta via Interfaces Fortemente Tipadas:**
   - Como os módulos residem dentro do mesmo monólito, as chamadas síncronas entre domínios ocorrem em memória por interfaces injetadas via injeção de dependência (`Dependency Injection`), eliminando serialização HTTP redundante.
2. **Eventos de Domínio Internos (In-Memory Event Bus):**
   - Eventos como `PaymentConfirmedEvent` ou `ProfileSuspendedEvent` são propagados através de um *Event Bus* assíncrono interno, desacoplando o módulo financeiro do paywall de mídia e das notificações.
