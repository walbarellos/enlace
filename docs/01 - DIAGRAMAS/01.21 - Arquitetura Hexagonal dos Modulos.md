# 01.21 — ARQUITETURA HEXAGONAL DOS MÓDULOS DE DOMÍNIO

> **Documento:** 01.21  
> **Fase:** Modelagem e Arquitetura (Modelo em Cascata)  
> **Classificação:** Arquitetura de Software / Clean Architecture  
> **Versão:** 2.0.0  
> **Status:** Aprovado  

---

## 1. Estrutura Canônica de um Bounded Context

O diagrama a seguir detalha a implementação do padrão **Ports & Adapters (Arquitetura Hexagonal)** aplicado a cada um dos 13 módulos do **Enlace Backend Core**:

```mermaid
flowchart TB
    subgraph AdaptadoresEntrada["Adaptadores de Entrada (Driving / Inbound)"]
        HTTPController["Controlador REST (Fastify/Express)"]
        CLICommand["Comando CLI / Job Interno"]
        EventListener["Consumidor de Eventos de Domínio"]
    end

    subgraph CamadaAplicacao["Camada de Aplicação (Application)"]
        UseCase["Casos de Uso (Commands / Queries)"]
        PortaEntrada["Porta de Entrada (Interface de Serviço)"]
    end

    subgraph NucleoDominio["Núcleo de Domínio Puro (Domain - Sem Dependências)"]
        Entidades["Entidades de Domínio & Agregados"]
        ValueObjects["Objetos de Valor Imutáveis"]
        RegrasNegocio["Regras de Domínio & Policies"]
        EventosDominio["Eventos de Domínio Puros"]
        PortaSaida["Porta de Saída (Interface de Repositório)"]
    end

    subgraph AdaptadoresSaida["Adaptadores de Saída (Driven / Outbound)"]
        RepoPostgres["Repositório PostgreSQL (Drizzle/Prisma)"]
        StorageS3["Driver S3 / MinIO (Uploads Pré-assinados)"]
        PixGateway["Adaptador de Gateway Financeiro (Pix)"]
        RedisCache["Adaptador de Cache e Rate Limiter (Redis)"]
    end

    %% Fluxos de Invocação
    HTTPController --> PortaEntrada
    CLICommand --> PortaEntrada
    EventListener --> PortaEntrada
    PortaEntrada --> UseCase

    UseCase --> Entidades
    UseCase --> ValueObjects
    UseCase --> RegrasNegocio
    UseCase --> EventosDominio
    UseCase --> PortaSaida

    PortaSaida -.->|Inversão de Controle| RepoPostgres
    PortaSaida -.->|Inversão de Controle| StorageS3
    PortaSaida -.->|Inversão de Controle| PixGateway
    PortaSaida -.->|Inversão de Controle| RedisCache
```

---

## 2. Inversão de Controle e Testabilidade

1. **Testes Unitários Puros e Instantâneos:**
   - A camada `Domain` e `Application` é testada sem subir nenhum banco de dados ou container. Repositórios e gateways externos são substituídos por fakes em memória em questão de milissegundos.
2. **Independência de Framework:**
   - A migração futura de um framework HTTP (ex: de Express para Fastify ou Hono) impacta unicamente a pasta `Presentation`, deixando o core de negócio $100\%$ intocado.
