# 01.02 — DIAGRAMAS DE CASOS DE USO (UML USE CASES)

> **Documento:** 01.02  
> **Fase:** Modelagem e Arquitetura (Modelo em Cascata)  
> **Classificação:** Modelagem Funcional / UML  
> **Versão:** 1.0.0  
> **Status:** Aprovado  

---

## 1. Diagrama de Casos de Uso Geral

```mermaid
flowchart LR
    %% Atores
    subgraph Atores["Atores"]
        Visitante["Visitante Anônimo"]
        Cliente["Cliente (Pseudônimo)"]
        Acomp["Acompanhante"]
        Admin["Auditor / Admin"]
    end

    %% Casos de Uso
    subgraph CasosDeUso["Casos de Uso da Plataforma Enlace"]
        UC01(["UC-01: Confirmar Maioridade (+18)"])
        UC02(["UC-02: Submeter Documentos e Liveness"])
        UC03(["UC-03: Gerenciar Perfil Artístico"])
        UC04(["UC-04: Cadastrar Atributos de Acessibilidade"])
        UC05(["UC-05: Buscar por Região e Compatibilidade"])
        UC06(["UC-06: Emitir Cartão de Solicitação"])
        UC07(["UC-07: Comprar Mídia Privada via Pix"])
        UC08(["UC-08: Contratar Plano de Destaque"])
        UC09(["UC-09: Avaliar Bilateralmente"])
        UC10(["UC-10: Submeter Alerta de Risco Confidencial"])
        UC11(["UC-11: Submeter Denúncia Comunitária"])
        UC12(["UC-12: Auditar Quarentena Preventiva"])
    end

    %% Relações Visitante
    Visitante --> UC01
    Visitante --> UC05

    %% Relações Cliente
    Cliente --> UC05
    Cliente --> UC06
    Cliente --> UC07
    Cliente --> UC09
    Cliente --> UC10
    Cliente --> UC11

    %% Relações Acompanhante
    Acomp --> UC01
    Acomp --> UC02
    Acomp --> UC03
    Acomp --> UC04
    Acomp --> UC08
    Acomp --> UC09
    Acomp --> UC10
    Acomp --> UC11

    %% Relações Admin
    Admin --> UC12

    %% Dependências internas
    UC02 -.->|"<<include>>"| UC01
    UC06 -.->|"<<extend>>"| UC05
    UC10 -.->|"<<include>>"| UC09
    UC12 -.->|"<<include>>"| UC11
```

---

## 2. Relações Estruturais dos Casos de Uso

1. **`<<include>>` de Maioridade em KYC:**
   - O cadastro e envio de documentos (`UC-02`) inclui compulsoriamente a validação estrita de maioridade civil (`UC-01`).
2. **`<<extend>>` de Solicitação a partir da Busca:**
   - A emissão do cartão de solicitação e redirecionamento direto (`UC-06`) estende o fluxo de busca e descoberta de perfis compatíveis (`UC-05`).
3. **`<<include>>` de Alerta Confidencial de Segurança:**
   - Ao emitir uma avaliação bilateral (`UC-09`), o usuário tem a opção de incluir um relato confidencial de risco (`UC-10`), processado exclusivamente pelo motor de reputação sem exibição pública.
