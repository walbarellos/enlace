# 01.11 — ARQUITETURA DE IMPLANTAÇÃO (DEPLOYMENT / INFRAESTRUTURA)

> **Documento:** 01.11  
> **Fase:** Modelagem e Arquitetura (Modelo em Cascata)  
> **Classificação:** Engenharia de Infraestrutura / DevOps & Cloud  
> **Versão:** 1.0.0  
> **Status:** Aprovado  

---

## 1. Diagrama de Implantação Física / Cloud

O diagrama a seguir descreve a topologia de nós, containers e serviços gerenciados em nuvem para sustentar alta disponibilidade e resiliência:

```mermaid
flowchart TB
    %% Clientes Finais
    subgraph Dispositivos["Dispositivos dos Usuários"]
        UserBrowser["Navegador Desktop / Smartphone (4G/5G/Fibra)"]
    end

    %% Borda de Rede
    subgraph BordaEdge["Camada Edge / CDN Global"]
        Cloudflare["<b>Cloudflare Edge Network</b><br/>• Terminação SSL/TLS 1.3<br/>• WAF e Mitigação DDoS<br/>• Cache Estático nos POPs de SP, RJ e Brasília<br/>• Rate Limiting de Borda"]
    end

    %% Zona de DMZ / Ingress
    subgraph ClusterK8s["Cluster de Aplicação (Kubernetes / Containers)"]
        Ingress["<b>Ingress Controller (NGINX / Envoy)</b><br/>Distribui tráfego e gerencia roteamento HTTP/2"]
        
        subgraph PodsFrontend["Pods de Frontend"]
            NextApp1["Next.js SSR Pod 1"]
            NextApp2["Next.js SSR Pod 2"]
        end

        subgraph PodsBackend["Pods do Backend Core"]
            APICore1["Enlace Core Pod 1 (Node.js)"]
            APICore2["Enlace Core Pod 2 (Node.js)"]
            APICoreN["Enlace Core Pod N (Auto-scaling)"]
        end

        subgraph PodsWorkers["Pods de Processamento"]
            WorkerMedia["Media Worker Pod (FFmpeg / Sharp)"]
        end
    end

    %% Camada de Dados Isolada
    subgraph CamadaDados["Camada de Dados Gerenciada (VPC Privada)"]
        PostgresPrimario[("<b>PostgreSQL 16 Primary</b><br/>• ACID<br/>• PostGIS<br/>• Esquema Público")]
        PostgresReplica[("<b>PostgreSQL 16 Read Replica</b><br/>• Consultas de Catálogo")]
        PostgresVault[("<b>PostgreSQL 16 Vault Schema</b><br/>• Criptografia AES-256<br/>• Dados Civis Isolados")]
        RedisCluster[("<b>Redis 7 Cluster</b><br/>• Sessões, Rate Limit e Filas")]
        S3Storage[("<b>Storage de Objetos Criptografado</b><br/>• Buckets S3 Privados para Mídias")]
    end

    %% Relações de Tráfego
    UserBrowser -->|"HTTPS"| Cloudflare
    Cloudflare -->|"Túnel Criptografado Seguro"| Ingress
    Ingress --> NextApp1
    Ingress --> NextApp2
    Ingress --> APICore1
    Ingress --> APICore2
    Ingress --> APICoreN

    APICore1 --> PostgresPrimario
    APICore2 --> PostgresPrimario
    APICoreN --> PostgresReplica
    APICore1 --> PostgresVault
    APICore1 --> RedisCluster
    APICore2 --> RedisCluster

    RedisCluster -->|"Consome Fila BullMQ"| WorkerMedia
    WorkerMedia --> S3Storage
    APICore1 --> S3Storage
```

---

## 2. Destaques de Segurança de Rede (VPC e Isolamento)

1. **VPC Privada Sem Acesso Público:**
   - O banco de dados PostgreSQL e as instâncias Redis não possuem IP público, sendo acessíveis exclusivamente a partir da rede privada interna dos pods da aplicação.
2. **Separação de Leitura e Escrita:**
   - As consultas intensivas de busca de acompanhantes e filtros por acessibilidade são direcionadas para a réplica de leitura (`Read Replica`), desonerando o nó primário para transações financeiras e cadastros.
3. **Escalonamento Automático (HPA):**
   - Os pods do Backend Core escalam horizontalmente com base no consumo de CPU e latência média das requisições via *Horizontal Pod Autoscaler* (HPA).
