# 01.04 — ARQUITETURA C4 — NÍVEL 2: CONTAINERS DA APLICAÇÃO

> **Documento:** 01.04  
> **Fase:** Modelagem e Arquitetura (Modelo em Cascata)  
> **Classificação:** Arquitetura C4 / Nível 2  
> **Versão:** 1.0.0  
> **Status:** Aprovado  

---

## 1. Diagrama C4 — Nível 2: Containers

O diagrama de containers apresenta os componentes de tempo de execução, bancos de dados e serviços auxiliares que formam a infraestrutura da **Plataforma Enlace**:

```mermaid
flowchart TB
    %% Usuários
    Cliente["Cliente / Acompanhante / Visitante<br/>[Navegador / Mobile Web]"]

    %% Perímetro Edge
    subgraph PerimetroEdge["Perímetro de Borda & Segurança"]
        CDN["<b>Cloudflare Edge & WAF</b><br/>[Container: Edge/CDN]<br/>Terminação TLS 1.3, mitigação de DDoS, WAF, cache de páginas estáticas e taxa de requisições."]
    end

    %% Camada de Apresentação
    subgraph CamadaFrontend["Camada de Apresentação"]
        Frontend["<b>Web App Enlace</b><br/>[Container: Next.js / TypeScript]<br/>Interface acessível (WCAG 2.2 AA), SSR para indexação rápida e componentes Radix UI inclusivos."]
    end

    %% Camada de Aplicação Core
    subgraph CamadaBackend["Camada de Aplicação (Core)"]
        BackendCore["<b>Enlace Backend Core</b><br/>[Container: Node.js / Modular Monolith]<br/>Executa as regras de domínio, autenticação JWT, cálculo de matching de acessibilidade e orquestração de pagamentos."]
        MediaWorker["<b>Worker de Processamento de Mídia</b><br/>[Container: Node.js / FFmpeg / Libvips]<br/>Transcodifica WebP/AVIF, gera streaming HLS adaptativo e injeta marca d'água esteganográfica."]
    end

    %% Camada de Dados e Persistência
    subgraph CamadaPersistencia["Camada de Armazenamento e Estado"]
        Postgres[("<b>PostgreSQL 16 Cluster</b><br/>[Container: Banco Relacional + PostGIS]<br/>Persiste perfis, catálogos, atributos de acessibilidade JSONB, transações financeiras e cofre civil criptografado.")]
        RedisStore[("<b>Redis 7 Cache & Queue</b><br/>[Container: In-Memory Data Store]<br/>Armazena sessões ativas, controle de rate limiting em tempo real e fila de processamento de mídias.")]
        ObjectStorage[("<b>Storage de Objetos</b><br/>[Container: MinIO / S3 Compatível]<br/>Guarda segura de arquivos brutos e processados com encriptação AES-256.")]
    end

    %% Relações
    Cliente -->|"HTTPS / TLS 1.3"| CDN
    CDN -->|"Encaminha requisições"| Frontend
    Frontend -->|"Chamadas de API [JSON/HTTPS]"| BackendCore
    BackendCore -->|"Leitura e Escrita Transacional [SQL]"| Postgres
    BackendCore -->|"Cache de catálogo e Rate Limit [TCP]"| RedisStore
    BackendCore -->|"Enfileira jobs de transcodificação [BullMQ]"| RedisStore
    RedisStore -->|"Consome tarefas assíncronas"| MediaWorker
    MediaWorker -->|"Lê originais e grava mídias otimizadas"| ObjectStorage
    BackendCore -->|"Gera URLs pré-assinadas com expiração"| ObjectStorage
```

---

## 2. Racional das Decisões de Containers

1. **Next.js com Server-Side Rendering (SSR):**
   - Viabiliza renderização ultrarrápida do catálogo público, fundamental para conexões móveis variáveis no Acre e em outras regiões do Brasil, assegurando conformidade estrita de acessibilidade no HTML inicial.
2. **Worker Especializado de Mídia Desacoplado via Redis:**
   - O processamento pesado de vídeos (conversão para HLS e injeção de esteganografia frame a frame) não bloqueia as threads do servidor de APIs, operando de forma assíncrona por meio de filas gerenciadas no Redis.
3. **PostgreSQL com Esquema de Cofre Dedicado:**
   - Um único cluster relacional consolidado reduz drasticamente os custos operacionais e a complexidade de manutenção, mantendo a segregação de segurança através de esquemas isolados (`public` vs `vault_civil`) e regras de Row-Level Security (RLS).
