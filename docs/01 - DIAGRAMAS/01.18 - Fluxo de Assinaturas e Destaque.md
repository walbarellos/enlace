# 01.18 — FLUXO DE ASSINATURAS, PLANOS E DESTAQUE NO RANKING

> **Documento:** 01.18  
> **Fase:** Modelagem e Arquitetura (Modelo em Cascata)  
> **Classificação:** Engenharia de Produto / Algoritmo de Ranking  
> **Versão:** 1.0.0  
> **Status:** Aprovado  

---

## 1. Estrutura de Níveis e Multiplicadores de Destaque

O algoritmo de posicionamento no catálogo do Enlace combina a localização aproximada com a modalidade de assinatura contratada pelo acompanhante:

```mermaid
flowchart LR
    subgraph PlanosDisponiveis["Tiers de Assinatura do Prestador"]
        Free["<b>Gratuito (Nível 3)</b><br/>• Posição Orgânica Padrão<br/>• Multiplicador: 1.0x<br/>• Galeria: até 6 fotos"]
        Bronze["<b>Plano Bronze</b><br/>• Multiplicador: 1.5x<br/>• Selo Bronze<br/>• Galeria: até 12 fotos"]
        Prata["<b>Plano Prata</b><br/>• Multiplicador: 2.5x<br/>• Selo Prata<br/>• Galeria: até 20 fotos + 2 teasers"]
        Ouro["<b>Plano Ouro</b><br/>• Multiplicador: 4.0x<br/>• Selo Ouro VIP<br/>• Topo do Bairro nas buscas"]
        Diamante["<b>Plano Diamante</b><br/>• Multiplicador: 8.0x<br/>• Selo Diamante Exclusivo<br/>• Vitrine Principal da Cidade"]
    end

    Free --> Bronze --> Prata --> Ouro --> Diamante
```

---

## 2. Diagrama de Sequência da Ativação de Destaque

```mermaid
sequenceDiagram
    autonumber
    actor Acomp as Acompanhante
    participant Web as Painel do Prestador
    participant SubAPI as Módulo de Assinaturas
    participant Gateway as Gateway de Pagamento
    participant SearchIdx as Índice de Busca e Ranking

    Acomp->>Web: Seleciona Plano "Ouro Mensal" (R$ 149,00)
    Web->>SubAPI: POST /api/v1/subscriptions/subscribe {planCode: "OURO_MENSAL"}
    SubAPI->>Gateway: Cria cobrança recorrente / Pix
    Gateway-->>Web: Retorna QRCode Pix instantâneo
    Acomp->>Gateway: Realiza o pagamento no app bancário
    
    Gateway->>SubAPI: Webhook: Pagamento Confirmado
    SubAPI->>SubAPI: Atualiza registro: status = ACTIVE, expires_at = now() + 30 dias
    SubAPI->>SearchIdx: Atualiza peso do perfil: multiplier = 4.0, tier = "OURO"
    SearchIdx-->>SearchIdx: Reindexa posição no catálogo em memória (Redis)
    
    SubAPI-->>Web: Dispara notificação: "Plano Ouro Ativado com Sucesso!"
    Web-->>Acomp: Exibe perfil promovido ao topo das buscas da região
```

---

## 3. Algoritmo de Pontuação de Ranking (Score Final)

A fórmula de ordenação determinística das listagens é calculada segundo a seguinte equação:

$$\text{RankingScore} = (\text{BaseScore} \times \text{TierMultiplier}) + (\text{AccessibilityMatch} \times 50) + (\text{ReviewScore} \times 10) - (\text{DistanceKm} \times 2)$$

- **Onde:**
  - $\text{TierMultiplier} \in \{1.0, 1.5, 2.5, 4.0, 8.0\}$;
  - $\text{AccessibilityMatch} \in [0, 1]$ (adiciona até 50 pontos extras quando o perfil atende perfeitamente aos requisitos funcionais do cliente);
  - $\text{ReviewScore} \in [1, 5]$ (média ponderada de avaliações públicas);
  - $\text{DistanceKm}$ penaliza proporcionalmente a distância do centro da busca sem quebrar a privacidade.
