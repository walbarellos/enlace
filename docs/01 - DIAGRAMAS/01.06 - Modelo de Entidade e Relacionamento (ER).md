# 01.06 — MODELO DE ENTIDADE E RELACIONAMENTO (ERD)

> **Documento:** 01.06  
> **Fase:** Modelagem e Arquitetura (Modelo em Cascata)  
> **Classificação:** Engenharia de Dados / Modelagem Relacional  
> **Versão:** 1.0.0  
> **Status:** Aprovado  

---

## 1. Diagrama de Entidade e Relacionamento (ERD)

O diagrama a seguir representa as principais entidades do banco de dados relacional (PostgreSQL), evidenciando a separação estrita entre perfis sociais, motor de acessibilidade, transações financeiras e o cofre de dados civis:

```mermaid
erDiagram
    ACCOUNTS ||--o| CLIENT_PROFILES : "possui"
    ACCOUNTS ||--o| PROVIDER_PROFILES : "possui"
    ACCOUNTS ||--o| CIVIL_VAULT_IDENTITIES : "vincula isolado"

    PROVIDER_PROFILES ||--o{ ACCESSIBILITY_OFFERINGS : "oferece"
    CLIENT_PROFILES ||--o{ ACCESSIBILITY_PREFERENCES : "demanda"

    PROVIDER_PROFILES ||--o{ MEDIA_ASSETS : "publica"
    PROVIDER_PROFILES ||--o{ SUBSCRIPTIONS : "contrata"
    
    CLIENT_PROFILES ||--o{ SERVICE_REQUESTS : "emite"
    PROVIDER_PROFILES ||--o{ SERVICE_REQUESTS : "recebe"

    SERVICE_REQUESTS ||--o| REVIEWS : "origina"
    
    CLIENT_PROFILES ||--o{ ORDERS : "compra"
    PROVIDER_PROFILES ||--o{ ORDERS : "fornece"
    ORDERS ||--o| MEDIA_ASSETS : "adquire item"

    ACCOUNTS ||--o{ CONFIDENTIAL_SAFETY_LOGS : "registra incidente"

    ACCOUNTS {
        uuid id PK
        string email UK
        string phone
        string role "CLIENT, PROVIDER, ADMIN"
        int verification_level "0, 1, 2, 3, 4"
        string status "ACTIVE, SUSPENDED, DELETED"
        timestamp created_at
    }

    CLIENT_PROFILES {
        uuid id PK
        uuid account_id FK
        string pseudonym "Cliente_XXXX"
        string avatar_preset
        jsonb functional_needs
        timestamp created_at
    }

    PROVIDER_PROFILES {
        uuid id PK
        uuid account_id FK
        string artistic_name
        text bio
        string state_uf
        string city
        string neighborhood
        geometry fuzzy_point "Raio ofuscado 500m"
        int min_rate_cents
        string active_plan_tier "FREE, BRONZE, PRATA, OURO, DIAMANTE"
        boolean is_verified
        string status "ACTIVE, QUARANTINED, INACTIVE"
    }

    CIVIL_VAULT_IDENTITIES {
        uuid id PK
        uuid account_id FK
        bytea encrypted_full_name "AES-256"
        bytea encrypted_cpf "AES-256"
        date encrypted_birthdate
        bytea encrypted_pix_key
        float liveness_score
        string verification_status "PENDING, APPROVED, REJECTED"
        timestamp verified_at
    }

    ACCESSIBILITY_OFFERINGS {
        uuid id PK
        uuid provider_id FK
        string category "COMMUNICATION, MOBILITY, SENSORY, SUPPORT"
        string feature_code "COMM_LIBRAS, MOB_RAMP, NEURO_LIGHT, etc"
        boolean is_verified
    }

    ACCESSIBILITY_PREFERENCES {
        uuid id PK
        uuid client_id FK
        string category
        string feature_code
        int priority_weight "1 a 5"
    }

    MEDIA_ASSETS {
        uuid id PK
        uuid provider_id FK
        string media_type "PHOTO, VIDEO"
        string access_tier "PUBLIC, PAYWALL"
        int price_cents
        string storage_key
        string p_hash "Perceptual Hash Anti-CSAM"
        string status "PROCESSING, ACTIVE, BLOCKED"
    }

    ORDERS {
        uuid id PK
        uuid client_id FK
        uuid provider_id FK
        uuid media_asset_id FK
        int total_cents
        int platform_fee_cents "15%"
        int provider_net_cents "85%"
        string payment_method "PIX, CREDIT_CARD"
        string status "PENDING, PAID, FAILED, REFUNDED"
        timestamp paid_at
    }

    SUBSCRIPTIONS {
        uuid id PK
        uuid provider_id FK
        string plan_code "OURO_MENSAL"
        timestamp starts_at
        timestamp expires_at
        string status "ACTIVE, EXPIRED, CANCELLED"
    }

    SERVICE_REQUESTS {
        uuid id PK
        uuid client_id FK
        uuid provider_id FK
        timestamp preferred_datetime
        int duration_hours
        string location_type "PROVIDER_PLACE, CLIENT_PLACE, HOTEL"
        string status "CREATED, FORWARDED, COMPLETED, CANCELLED"
    }

    REVIEWS {
        uuid id PK
        uuid service_request_id FK
        int rating_score "1 a 5"
        text comment
        boolean is_published
        timestamp created_at
    }

    CONFIDENTIAL_SAFETY_LOGS {
        uuid id PK
        uuid reporter_account_id FK
        uuid target_account_id FK
        string incident_code "ABUSIVE_BEHAVIOR, CONSENT_BREACH, SCAM"
        int risk_weight "1 a 10"
        timestamp created_at
    }
```

---

## 2. Destaques da Modelagem de Dados

1. **Separação Criptográfica do Cofre (`CIVIL_VAULT_IDENTITIES`):**
   - Os dados civis não estão em colunas de texto claro na tabela `ACCOUNTS`. Eles são armazenados em esquema segregado com colunas tipadas como `bytea` sob criptografia de chave gerenciada (AES-256).
2. **Normalização dos Atributos de Acessibilidade:**
   - As tabelas `ACCESSIBILITY_OFFERINGS` e `ACCESSIBILITY_PREFERENCES` permitem que o motor de compatibilidade execute junções rápidas via índices relacionais e operadores de conjuntos.
3. **Geometria Difusa (`fuzzy_point`):**
   - A coluna de geolocalização utiliza o tipo `geometry` do PostGIS, alimentada com uma dispersão estocástica deliberada para garantir que as coordenadas reais jamais fiquem expostas.
